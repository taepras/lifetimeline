const BASE_UNIT = 8;

export default {
    spacing: {
        xxs: BASE_UNIT / 4,
        xs: BASE_UNIT / 2,
        x1: BASE_UNIT,
        x2: BASE_UNIT * 2,
        x3: BASE_UNIT * 3,
        x4: BASE_UNIT * 4,
    }
};